# config.toml

# git.exe のパス（Windows の例）
git_path = "C:\\Program Files\\Git\\bin\\git.exe"

[[commands]]
name = "Status"
cmd  = "status -sb"

[[commands]]
name = "Graph"
cmd  = "log --oneline --graph --decorate --all --color=always"

[[commands]]
name = "Fetch"
cmd  = "fetch --all --prune"

[[commands]]
name = "Show Branches"
cmd  = "branch -vv"

